secret_num = 9
guess_count = 0
guess_Limit = 3
while guess_count < guess_Limit:
    guess = int(input("Guess the number: "))
    guess_count += 1
    if guess == secret_num:
        print ("you win")
        break
else:
    print ("Out of chances") 
  
